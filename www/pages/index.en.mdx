import Image from "next/image";
import logo from "../../assets/v2_4x.png";

export function Logo() {
  return (
    <div className="w-full h-[150px] relative">
      <Image
        src={logo.src}
        alt="Shimmer Shard logo"
        fill
        className="object-contain aspect-video"
      />
    </div>
  );
}

## 🎉 Hi, there! 🎉

<Logo />
### 🫣 What is ShimmerShards?{" "}

<b>ShimmerShards</b> is founded on the principles of simplicity and draws inspiration
from well-known libraries like Recoil and Jotai. Its central concept revolves around
the Atomic state model, which efficiently manages state by breaking it down into
granular units. What sets ShimmerShards apart is its minimalist APIs and functionalities.
It's incredibly straightforward to get started with – no complex configurations or
setups required. With just a simple installation, you can create a Shard (shared
state unit) and seamlessly integrate it into your project. Moreover, ShimmerShards
is fully type-safe for TypeScript users. This ensures a robust and reliable development
experience, free from type-related errors and bugs.

<b>ShimmerShards</b> is a ``simple-to-use`` React state management solution, reduces
unnecessary updates and lets you focus on creating an awesome user experience.

**Try it out!** and see how's `ShimmerShards bringing productivity to you.`
